<div>
  <h2>{{title}}</h2>
  {{#each products}}
    <p>
      id:
      {{this.id}}
      name:
      {{this.name}}
      price:
      {{this.price}}
      description:
      {{this.description}}
    </p>
    <hr />
  {{/each}}
</div>